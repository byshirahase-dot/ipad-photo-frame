# 認証情報は絶対にコミットしない
.env

node_modules/
logs/
data/suisen.pdf
*.png

# 刻み実行の一時ファイル（Cookieセッション・進行中ジョブ）
state/_session_*.json
state/_job.json
reports/.cache-*.json

# 単発予約（--title）の一時レポート。本当の記録は state/ の台帳＝コミット対象。
# 失敗時の内容は古くなりやすいのでGit管理しない。
reports/adhoc-*.md
